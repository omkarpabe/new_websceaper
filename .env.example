# Environment Configuration for Web Scraper Pro

# Node Environment (development, production)
NODE_ENV=development

# Server Configuration
PORT=5000

# Rate Limiting Configuration (optional overrides)
# RATE_LIMIT_WINDOW_MS=5000
# RATE_LIMIT_MAX_REQUESTS=1

# Database Configuration (future PostgreSQL support)
# DATABASE_URL=postgresql://username:password@localhost:5432/webscraper

# CORS Configuration (if needed for different domains)
# CORS_ORIGIN=http://localhost:3000